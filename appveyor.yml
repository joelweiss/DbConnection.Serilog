version: 1.0.0.{build}
configuration: Release
assembly_info:
  patch: true
  file: AssemblyInfo.*
  assembly_version: '{version}'
  assembly_file_version: '{version}'
  assembly_informational_version: '{version}'
before_build:
- ps: nuget restore Source\DbConnection.Serilog.sln
build:
  project: Source\DbConnection.Serilog.sln
  verbosity: minimal
after_build:
- ps: >-
    nuget pack Nuget\DbConnection.Serilog.nuspec -Version $env:APPVEYOR_BUILD_VERSION-Beta

    appveyor PushArtifact DbConnection.Serilog.$env:APPVEYOR_BUILD_VERSION-Beta.nupkg